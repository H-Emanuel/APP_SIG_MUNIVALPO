import 'package:flutter/material.dart';

class ConfiguracionMenu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Configuración'),
      ),
      body: Center(
        child: Text('Configuración del menú'),
      ),
    );
  }
}
